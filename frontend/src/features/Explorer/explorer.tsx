import * as React from "react"
import { withHub } from "../../Hub";

class Explorer extends React.Component {
    render(){
        return (
            <div>Explorer</div>
        )
    }
}

export default withHub(Explorer)